name: Build and Deploy to Hostinger Branch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Build Only for Verification ✅
        run: |
          npm install
          npm run build
          echo "Build successful. Deployment disabled per user request."
